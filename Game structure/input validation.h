#pragma once
#include <iostream>
#include <string>
namespace input_checker {
	int check_number(std::string s);
	//int is_input_valid(std::string s, int allowed_options);
	//int take_user_input(std::string s, int allowed_options);
}